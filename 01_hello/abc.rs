/*
 * Basic of basic: create a blank file named main.rs or abc.rs.
 * make sure we have a main function in this file as entry point
 * compile it in cli as: ```rustc ./abc.rs```
 * run the output binary file as: ```./abc```
 */
fn main() {
    /* 
     *```println!``` is a macro, which will be pre-compiled into a group lines of codes
     */
    println!("hola, hola");
}
